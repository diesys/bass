- fare poco js che crea al volo la tracklist? cosi da includere in script inline solo il necessario

- fare loading animation con i due colori (o solo uno e il suo invert? meglio) con due cerchi che vanno dx e sx (vedi codepen.io/diesys)